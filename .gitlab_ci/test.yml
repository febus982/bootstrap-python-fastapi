# Test Docker image
test:
  stage: test
  image: docker:24.0.2
  services:
    - docker:24.0.2-dind
  before_script:
    - apk add --no-cache bash
  script:
    - docker run --rm $TEST_TAG make ci-test
